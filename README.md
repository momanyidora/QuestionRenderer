## Getting started

1. Clone the repo
2. Run `npm install`
3. Copy `.env.example` to `.env.local` and fill in the values
4. Run `npm run dev`

## Branching

- Branch off `dev` for all features
- Name branches like `feature/hero-section`, `fix/navbar-bug`
- Open PRs into `dev`, never into `main`
